// 복사해서 Secrets.xcconfig 만들고 값 채우기 (Secrets.xcconfig는 gitignore됨).
// anon/publishable 키만. service_role 절대 금지(RLS로 보호되는 공개 키).
// xcconfig는 '//'를 주석으로 보므로 URL의 슬래시는 $() 트릭으로 깨준다.

SUPABASE_URL = https:/$()/YOUR-REF.supabase.co
SUPABASE_ANON_KEY = sb_publishable_xxx
